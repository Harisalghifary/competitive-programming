# Difficulty : hash table, array, count, bit manipulation
# Tag : Medium

def singleNumber(self, nums: List[int]) -> List[int]:
    # Map method
    # res = []
    # map = defaultdict(int)

    # for num in nums:
    #     map[num] += 1

    # for num in nums:
    #     if map[num]==1:
    #         res.append(num)
    
    # return res